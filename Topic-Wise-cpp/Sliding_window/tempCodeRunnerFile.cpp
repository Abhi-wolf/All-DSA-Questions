 // while (required >= sizeOfMap && start < n)
    // {
    //     if (mp.find(s[start]) != mp.end())
    //     {
    //         if (len >= (end - start + 1))
    //         {
    //             len = end - start + 1;
    //             ind = start;
    //         }

    //         mp[s[start]]++;

    //         if (mp[s[start]] > 0)
    //             required--;
    //     }
    //     cout << "end = " << end << "  start=" << start << " len=" << len << endl;
    //     start++;
    // }